---
description: 课评 AI 工程仓 · 身份、MVP 边界与协作硬约束
alwaysApply: true
---

# lesson-review-ai · AI 规则

## 身份与范围

- 用户为本仓**维护者**，职责为教学质量相关赋能与工具先行。
- 角色：质量架构师 / 发现者 / AI 先行者。
- 本仓做**课评教练流水线**（转写 → 纠错 → 结构 → 建议）；业务标准上游是 `teaching-quality-enablement`。
- 适用场景：线下现场培训与线上培训（具体品牌不在本仓公开文档中展开）。
- 授课媒介默认 **公屏（桌面共享）**：PPT、笔记软件、IDE 推送到学员屏幕；**不使用黑板板书**。报告改法禁止默认「板书」。

## 硬约束

- 三不三要：不公开批评校区负责人/教师；不越级指挥一线；不只带问题；要私下对齐；通过校区负责人推动；带方法。
- 报告对事不对人；区分合格线与水平线。
- 真实课例媒体、学员隐私、API Key **不进 Git**。
- 口径冲突以上游文档仓为准，回改本仓摘要与 prompts，不静默另立标准。
- 本仓为 **public** 仓库：文档与提交中**不写**维护者真名、所在公司/集团或对外品牌名。

## MVP（当前只做这些）

- 输入：1 个视频，或手动选定的同模块 3～5 个视频；按文件名前缀数字排序。
- 流水线：抽音轨 → 转写 → 纠错 → 结构要点（模块双层）→ 提升建议与改法。
- 第一用户：维护者本人；不做全员自查门户、不做 AI 成片、不做自动归模块。

## 技术默认（可被 ADR 修订）

- 语言与编排：Python CLI 单体（uv 管理依赖）。
- ASR：本机 mlx-whisper（默认 Apple Silicon）；其他平台另开 ADR。
- LLM：DeepSeek API（OpenAI 兼容）；备选通义千问。

## 文风

- 先可读，再短；完整句子；禁止电报体。
- 中英文（及中文与数字）之间保留空格；Markdown 以 Prettier + prettier-plugin-zh + markdownlint 为准。
- 文档命名：`数字_英文_中文.md`。

## 协作

- 改评价行为前：先读 `docs/01-product` 中的 PRD，再按「上游标准引用」打开上游对应 Markdown，最后改 `prompts/`。
- 不熟悉 uv / ffmpeg / 本地 CLI 时，可读 `docs/90-guides/`（学习区，非产品真相源）。
- 交付按**里程碑分支 / 多次提交 / 一个 PR**推进；Issue 可细拆，但不强制「一 Issue 一 PR」。
- 维护者确认前不擅自 commit / push / 合入。仅当维护者明确说出「提交」「commit」「推送」「合入」等授权语时才执行对应 git 操作；「按建议来 / 开干 / 实现」只授权改代码与开 Issue/分支，不授权提交。
- 文档分类对齐后再分批提交；给方案时附下一步；不把本仓扩成第二个标准文档仓。
- 改 Markdown 后本地跑 `npm run format` 与 `npm run lint:md`，确保无 Markdownlint 错误再提交。
